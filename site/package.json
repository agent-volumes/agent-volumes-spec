{
  "private": true,
  "scripts": {
    "dev": "mint dev",
    "validate": "mint validate",
    "broken-links": "mint broken-links",
    "lint": "mint validate && mint broken-links"
  },
  "devDependencies": {
    "mint": "^4.2.566"
  }
}
