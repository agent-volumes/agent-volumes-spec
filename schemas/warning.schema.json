{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agentvolumes.org/spec/0.1.0-draft.5/schemas/warning.schema.json",
  "title": "Agent Volumes Warning",
  "type": "object",
  "additionalProperties": true,
  "required": ["category"],
  "properties": {
    "category": {
      "type": "string",
      "pattern": "^(?!.*--)[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$",
      "description": "Core v0.1 categories are unknown-field, deprecated, migration, unknown-capability-field, unknown-capability-value, yanked-version, and noncanonical-entrypoint. Extension categories use the same slug-like syntax."
    },
    "message": { "type": "string" },
    "path": { "type": "string" },
    "field": { "type": "string" },
    "value": { "type": "string" }
  }
}
