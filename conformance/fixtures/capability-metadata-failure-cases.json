{
  "specVersion": "0.1.0-rc.1",
  "description": "Capability metadata Problem Details failure cases for portable Agent Volumes bibliothecas.",
  "fixtures": [
    {
      "name": "capability-metadata-rate-limited",
      "endpoint": "GET /api/v1/capabilities",
      "schema": "problem-details",
      "expected": { "failureCategory": "rate-limited" },
      "payload": {
        "type": "https://agentvolumes.org/problems/rate-limited",
        "title": "Rate limited",
        "status": 429,
        "detail": "The capability metadata request was rate limited."
      }
    }
  ]
}
